# Copy this file to .env and fill in your values (never commit .env)
# HuggingFace token — required only for gated models (e.g. LLaMA 3.2)
HF_TOKEN=

# Optional — OpenAI API for cloud/demo features if your app uses it
OPENAI_API_KEY=
